---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: brokers.platform.company.com
spec:
  group: platform.company.com
  names:
    kind: Broker
    listKind: BrokerList
    plural: brokers
    shortNames:
    - br
    singular: broker
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cloudProvider
      name: Provider
      type: string
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.activeDeployments
      name: Active
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Broker is the Schema for the brokers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BrokerSpec defines the desired state of Broker
            properties:
              authentication:
                description: Authentication configuration for broker communication
                properties:
                  secretRef:
                    description: SecretRef references a secret containing authentication
                      credentials
                    properties:
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - name
                    - namespace
                    type: object
                  type:
                    default: jwt
                    description: Type of authentication (jwt, mtls, api-key)
                    enum:
                    - jwt
                    - mtls
                    - api-key
                    - none
                    type: string
                required:
                - type
                type: object
              capabilities:
                description: Capabilities describes what resources this broker can
                  provision
                items:
                  description: BrokerCapability describes a resource type the broker
                    can provision
                  properties:
                    providers:
                      description: Providers lists the specific implementations available
                        (e.g., ["postgresql", "mysql"])
                      items:
                        type: string
                      type: array
                    regions:
                      description: Regions lists specific regions where this capability
                        is available
                      items:
                        type: string
                      type: array
                    resourceType:
                      description: ResourceType is the kind of resource (Database,
                        Cache, Topic, etc.)
                      type: string
                  required:
                  - resourceType
                  type: object
                type: array
              cloudProvider:
                description: CloudProvider specifies the cloud provider (azure, aws,
                  gcp, on-prem)
                enum:
                - azure
                - aws
                - gcp
                - on-prem
                type: string
              endpoint:
                description: Endpoint is the base URL of the broker API
                minLength: 1
                pattern: ^https?://.*
                type: string
              healthCheck:
                description: HealthCheck configuration
                properties:
                  endpoint:
                    default: /health
                    description: Endpoint is the health check path (e.g., "/health")
                    type: string
                  failureThreshold:
                    default: 3
                    description: FailureThreshold is consecutive failures before marking
                      unhealthy
                    format: int32
                    minimum: 1
                    type: integer
                  intervalSeconds:
                    default: 30
                    description: IntervalSeconds is how often to check health
                    format: int32
                    minimum: 5
                    type: integer
                  timeoutSeconds:
                    default: 5
                    description: TimeoutSeconds is the health check timeout
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              maxConcurrentDeployments:
                default: 10
                description: MaxConcurrentDeployments limits parallel deployments
                format: int32
                type: integer
              priority:
                default: 100
                description: Priority for broker selection (higher number = higher
                  priority)
                format: int32
                type: integer
              region:
                description: Region is the cloud region this broker manages (e.g.,
                  "eastus", "us-west-2")
                type: string
            required:
            - capabilities
            - cloudProvider
            - endpoint
            type: object
          status:
            description: BrokerStatus defines the observed state of Broker
            properties:
              activeDeployments:
                description: ActiveDeployments is the current number of active deployments
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest observations of the broker's
                  state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastHeartbeat:
                description: LastHeartbeat is the timestamp of the last successful
                  health check
                format: date-time
                type: string
              message:
                description: Message provides additional context about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation most recently
                  observed by the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current state of the broker
                enum:
                - Pending
                - Ready
                - Unhealthy
                - Offline
                - Unknown
                type: string
              version:
                description: Version is the broker software version
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
