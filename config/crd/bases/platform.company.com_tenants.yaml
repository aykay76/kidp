---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: tenants.platform.company.com
spec:
  group: platform.company.com
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    singular: tenant
  scope: Cluster
  versions:
  - name: v1
    served: true
    storage: true
    additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: Display Name
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.resourceCount.teams
      name: Teams
      type: integer
    - jsonPath: .status.currentSpend
      name: Spend
      type: number
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    schema:
      openAPIV3Schema:
        description: Tenant is the Schema for the tenants API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: TenantSpec defines the desired state of Tenant
            properties:
              displayName:
                description: DisplayName is the human-readable name of the tenant
                minLength: 1
                type: string
              description:
                description: Description provides additional context about the tenant
                type: string
              domain:
                description: Domain is an optional DNS or organizational domain for the tenant
                type: string
              contacts:
                description: Contacts lists primary contacts for the tenant
                items:
                  description: Contact represents a team contact
                  properties:
                    email:
                      description: Email address
                      type: string
                    name:
                      description: Name of the contact person
                      type: string
                    role:
                      description: Role of the contact
                      type: string
                    slack:
                      description: Slack channel or handle
                      type: string
                  required:
                  - email
                  - name
                  type: object
                type: array
              billingCode:
                description: BillingCode used for chargeback or invoicing
                type: string
              quotas:
                description: Quotas define tenant-wide limits
                properties:
                  maxTeams:
                    description: MaxTeams is the maximum number of teams allowed in this tenant
                    format: int32
                    type: integer
                  maxApplications:
                    description: MaxApplications is the maximum number of applications across the tenant
                    format: int32
                    type: integer
                  maxDatabases:
                    description: MaxDatabases is the maximum number of databases across the tenant
                    format: int32
                    type: integer
                type: object
            required:
            - displayName
            type: object
          status:
            description: TenantStatus defines the observed state of Tenant
            properties:
              phase:
                description: Phase represents the current lifecycle phase
                enum:
                - Active
                - Suspended
                - Archived
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  $ref: "#/components/schemas/io.k8s.apimachinery.pkg.apis.meta.v1.Condition"
                type: array
              resourceCount:
                description: ResourceCount tracks the number of resources owned by this tenant
                properties:
                  teams:
                    format: int32
                    type: integer
                  applications:
                    format: int32
                    type: integer
                  databases:
                    format: int32
                    type: integer
                required:
                - teams
                - applications
                - databases
                type: object
              currentSpend:
                description: CurrentSpend tracks current monthly spending
                type: number
              lastUpdated:
                description: LastUpdated is the last time the status was updated
                format: date-time
                type: string
            type: object
    subresources:
      status: {}
